{
  "name": "@web-agent/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@web-agent/protocol": "*",
    "@web-agent/recorder-core": "*"
  },
  "scripts": {
    "build": "npm run build -w @web-agent/recorder-core && tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
