{
  "name": "web-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "recorder": "node apps/recording-service/dist/start.js",
    "start:demo": "npm run build && node scripts/start-demo.mjs",
    "agent:smoke": "node scripts/agent-model-smoke.mjs",
    "postinstall": "npm run build",
    "build": "node scripts/build-workspaces.mjs",
    "test": "node --test scripts/*.test.mjs && npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test:e2e": "npm run build && playwright test",
    "verify": "npm run typecheck && npm test && npm run test:e2e",
    "test:safety-regressions": "npm run build && vitest run packages/safety/src/c0-regression.test.ts packages/safety/src/text-url-regression.test.ts packages/safety/src/parenthesis-regression.test.ts packages/recording-adapter/src/url-regression.test.ts packages/workflow-builder/src/c0-persistence.test.ts packages/workflow-builder/src/text-persistence.test.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "zod": "^4.1.0"
  }
}
