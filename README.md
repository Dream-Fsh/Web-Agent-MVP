# Web Agent

Local-first web workflow recording and replay for browser-based operations.

## Foundation commands

```powershell
npm install
npm run build
npm run typecheck
npm test
```

This repository currently contains only the Task 01 workspace foundation. The
protocol, fixture site, recorder, safety policy, and runner are introduced by
later tasks in the frozen implementation plan.
